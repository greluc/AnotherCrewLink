[package]
name = "apm-probe"
description = "P1+ experiment 2: does the echo canceller link, and does it cancel? Its 32-bit question was answered and is now moot."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
# sonora, not webrtc-audio-processing: the latter does not compile for either Windows
# target, and Windows is where the users are. See docs/rust-port/08-dependency-review.md
# section 2.2.
sonora = "0.2.0"

