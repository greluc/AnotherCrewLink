[package]
name = "acl-turn-framing"
description = "Splitting a TURN stream back into messages, for relaying over TCP and TLS."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# None, deliberately. This crate parses bytes that arrive from a TURN server over a
# network a hostile party may sit on, so it is the one place in the tree where the trusted
# computing base is worth counting -- and it is `std` and nothing else. It is also depended
# on by `vendor/webrtc`, which is outside the workspace: a leaf with no dependencies cannot
# make that edge into a cycle.
[dependencies]

[lints]
workspace = true
