Use where the client draws its own button rather than MUI's: "Reload" under an error, and anything that should read as part of the game's own chrome.

```jsx
<OutlineButton onClick={reload}>Reload</OutlineButton>
```

The hover border is `--accent-action` (#00ff00) and nothing else changes — no fill, no lift, no scale.
