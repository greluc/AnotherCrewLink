Two jobs in the client: explaining why a control is disabled, and holding a player's per-peer volume slider and mute toggle.

```jsx
<Tooltip title="Only the game host can change this!"><span><Checkbox label="Walls Block Audio" disabled /></span></Tooltip>
```
